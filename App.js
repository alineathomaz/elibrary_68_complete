import React, { Component } from 'react';
import BottomTabNavigator from './.expo/components/BottomTabNavigator';

export default class App extends Component {
  render() {
    return <BottomTabNavigator />;
  }
}